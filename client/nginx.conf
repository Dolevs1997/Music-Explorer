server {
    listen 3001; # Must match the EXPOSE port in your Dockerfile
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}